// Points the app at the hosted backend on Render. This is a real, stable
// URL (unlike the old ngrok tunnel) - it won't change on its own.
export const API_BASE_URL = "https://project-exchange-backend.onrender.com";
