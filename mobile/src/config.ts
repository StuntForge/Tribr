// Points the app at your local backend while developing.
//
// Your phone (running Expo Go) and this computer need to be on the same
// Wi-Fi network. "localhost" won't work from a phone, so this uses the
// computer's LAN IP address instead. If your Wi-Fi reconnects and your
// computer gets a new IP, update it here - run `ipconfig` (Windows) and
// look for "IPv4 Address".
export const API_BASE_URL = "http://192.168.1.203:4000";
